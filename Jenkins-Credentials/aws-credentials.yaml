credentials:
  system:
    domainCredentials:
      - credentials:
          - usernamePassword:
              scope: GLOBAL
              id: "aws-credentials"
              username: ""           # Access Key ID
              password: ""  # Secret Access Key
              description: "AWS credentials for Jenkins pipeline"
